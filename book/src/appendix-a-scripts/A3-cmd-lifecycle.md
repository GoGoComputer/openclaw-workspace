# A3. cmd 라이프사이클 (start/stop/update/clean/uninstall/self-update)

> 컨테이너의 일상적인 켜기·끄기·업데이트·청소·제거를 담당하는 여섯 개 스크립트. 모든 파일을 **각각 전문(全文) 으로** 수록합니다.

## A3-1. `cmd/start.sh`

```bash
{{#include ../../../openclaw-mgr/cmd/start.sh}}
```

## A3-2. `cmd/stop.sh`

```bash
{{#include ../../../openclaw-mgr/cmd/stop.sh}}
```

## A3-3. `cmd/update.sh`

```bash
{{#include ../../../openclaw-mgr/cmd/update.sh}}
```

## A3-4. `cmd/clean.sh`

```bash
{{#include ../../../openclaw-mgr/cmd/clean.sh}}
```

## A3-5. `cmd/uninstall.sh`

```bash
{{#include ../../../openclaw-mgr/cmd/uninstall.sh}}
```

## A3-6. `cmd/self-update.sh`

```bash
{{#include ../../../openclaw-mgr/cmd/self-update.sh}}
```

---

다음 → [A4. cmd 데이터·운영](A4-cmd-data.md)
