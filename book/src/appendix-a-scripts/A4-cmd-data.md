# A4. cmd 데이터·운영 (backup/restore/models/logs/menu/network/schedule)

> 데이터 백업·복구, 모델 관리, 로그 조회, 대화형 메뉴, 네트워크 모드 전환, 스케줄러를 담당하는 일곱 개 스크립트. 모두 전문(全文) 으로 수록합니다.

## A4-1. `cmd/backup.sh`

```bash
{{#include ../../../openclaw-mgr/cmd/backup.sh}}
```

## A4-2. `cmd/restore.sh`

```bash
{{#include ../../../openclaw-mgr/cmd/restore.sh}}
```

## A4-3. `cmd/models.sh`

```bash
{{#include ../../../openclaw-mgr/cmd/models.sh}}
```

## A4-4. `cmd/logs.sh`

```bash
{{#include ../../../openclaw-mgr/cmd/logs.sh}}
```

## A4-5. `cmd/menu.sh`

```bash
{{#include ../../../openclaw-mgr/cmd/menu.sh}}
```

## A4-6. `cmd/network.sh`

```bash
{{#include ../../../openclaw-mgr/cmd/network.sh}}
```

## A4-7. `cmd/schedule.sh`

```bash
{{#include ../../../openclaw-mgr/cmd/schedule.sh}}
```

---

다음 → [A5. lib/common.sh](A5-lib-common.md)
